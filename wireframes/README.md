# Wireframes

Self-contained HTML mocks for UX review **before** writing `specs/` or application code.

| File | Purpose |
|------|---------|
| [app.html](app.html) | Main 3-step wizard + gallery (toggle via top tabs) |

Open in any browser:

```bash
xdg-open wireframes/app.html
```

Production UI will live under `src/spacemaker/adapters/inbound/web/static/` and should match approved wireframes.
